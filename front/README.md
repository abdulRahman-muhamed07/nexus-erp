# Nexus ERP Frontend

Frontend workspace for Nexus ERP.

- `html/` — application pages
- `css/` — stylesheets
- `js/` — JavaScript modules
- `assets/` — images, icons and static assets

Frontend code should stay isolated from the backend under `backend/`.
